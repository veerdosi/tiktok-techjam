{
  "name": "techjam-workbench-demo",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "sync:data": "node scripts/sync-data.mjs",
    "dev": "npm run sync:data && vite",
    "build": "npm run sync:data && tsc -b && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "vite": "latest",
    "typescript": "latest",
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {
    "@types/react": "latest",
    "@types/react-dom": "latest"
  }
}
